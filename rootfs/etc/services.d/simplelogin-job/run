#!/command/with-contenv bash
# shellcheck shell=bash
set -euo pipefail
source /etc/simplelogin-aio/env-helpers.sh
normalize_simplelogin_presence_env_vars

until curl -fsS http://127.0.0.1:7777/health >/dev/null; do
    echo "Waiting for SimpleLogin web service before starting job runner..."
    sleep 2
done

cd /code || exit 1
exec python job_runner.py
