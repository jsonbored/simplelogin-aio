name: Publish Release / SimpleLogin-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-publish-release.yml@7bfda522056a626fe401b734fbdd97239c0955b2
    permissions:
      actions: read
      contents: write
    with:
      release_name: SimpleLogin-AIO
      component: ""
      workflow_selector: build.yml
    secrets: inherit
