name: Publish Release / SimpleLogin-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-publish-release.yml@6f895a48251e0478918664968b19542b85056ed2
    permissions:
      actions: read
      contents: write
    with:
      release_name: SimpleLogin-AIO
      component: ""
      workflow_selector: build.yml
    secrets: inherit
