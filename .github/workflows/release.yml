name: Prepare Release / SimpleLogin-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@3b8613bf305a8bbebfe9f61636db935c303fe2cd
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: SimpleLogin-AIO
      component: ""
      component_label: ""
      previous_tag_command: latest-aio-tag
    secrets: inherit
