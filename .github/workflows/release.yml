name: Prepare Release / SimpleLogin-AIO

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  prepare-release:
    uses: JSONbored/aio-fleet/.github/workflows/aio-prepare-release.yml@c8cfa2e0d5e02e5c22e54647a7f32310a10acfac
    permissions:
      contents: write
      pull-requests: write
    with:
      release_name: SimpleLogin-AIO
      component: ""
      component_label: ""
      previous_tag_command: latest-aio-tag
    secrets: inherit
